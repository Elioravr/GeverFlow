= simple_form_for @board, html: { :class => 'form-horizontal' } do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.association :user_group, :input_html => { class: 'user-group-select'}

  .form-actions
    = f.button :submit, class: "btn btn-primary"
